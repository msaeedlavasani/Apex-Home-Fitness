import AnalyticsPage from '../../AnalyticsPage';

/**
 * /dashboard/analytics — dashboard sub-route.
 * Renders the AnalyticsPage component (see src/app/[locale]/AnalyticsPage.tsx).
 */
export default function AnalyticsRoute() {
  return <AnalyticsPage />;
}
