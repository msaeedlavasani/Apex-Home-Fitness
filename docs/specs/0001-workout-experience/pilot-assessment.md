# Pilot Assessment — Spec Kit Stage 4 (Workout Experience Specification)

> **STATUS: PILOT ARTIFACT — evaluation record (2026-09-14, final)**
> Pilot-only output (Stage 4, D2). Changes nothing in global governance or templates. Findings are recommendations only.

| Field | Value |
|---|---|
| Pilot | Workout Experience Specification (`docs/specs/0001-workout-experience/`) |
| Pilot class | `STANDARD` (docs-only; implementation expected CRITICAL later) |
| Date | 2026-09-14 · Baseline main `80b6eb5` · Prototype evidence DEV `prototype/workout-layout-blueprint` @ `a62a7ce` |
| Outcome | `SPEC_READINESS = READY`; remaining blocking product decisions = **NONE**; implementation NOT authorized |

## Evaluation

| Question | Finding |
|---|---|
| Was existing governance duplicated? | **No.** The spec routes by reference (traceability §19); `docs/TASKS.md` stayed the only backlog; the constitution was not restated. One structural cost: the spec's source table inevitably enumerates canonical docs — routing, not duplication. |
| Did the spec clarify product intent? | **Yes, materially.** A registered advisory vision (`WORKOUT-EXPERIENCE-V2.md`, "NOT YET IMPLEMENTED") plus a prototype exploration became a labeled contract: principles CONFIRMED against `PRODUCT-VISION.md`, direction CANDIDATE, and 13 blocking decisions surfaced explicitly (U-1…U-13). The largest clarification: separating *exploration* from *intent*. |
| Did prototype evidence stay separate from requirements? | **Yes.** Prototype content lives only in §14 (evidence table with an explicit `Status` column carrying `EXISTING` / `ACCEPTANCE FINDING` labels) and §15 (explicit non-requirements). No prototype behavior appears as CONFIRMED. Three.js, the GLB asset, the CSP relaxation and the iOS bridge are all recorded as implementation details, not requirements. |
| Did the process create unnecessary overhead? | **Moderate.** The mandated section set produced a **229-line** spec (vs the STANDARD guidance of ≤ ~150 lines). The length is content-driven (behavior surface + evidence separation + unknowns), not padding — but the STANDARD line budget does not fit experience-scale specs. |
| Independent review outcome | **PASS after correction.** Reviewer findings: 3 structural contradictions (STANDARD label vs owner-mandated CRITICAL-shaped artifact set; missing ADR-0005 mobile-posture declaration; section structure diverging from the Stage-1 template), 1 unsupported citation (V2 §26), 3 hidden-as-assumption items (CONFIRMED-by-implementation rows; code paths as requirement sources; imperative tasks.md phrasing), 2 broken/imprecise references. All corrected in the same change; 0 unresolved contradictions remain. |
| Was the STANDARD workflow size appropriate? | **Class: yes. Size: exceeded.** Docs-only, no cross-plane risk → STANDARD was correct. The artifact-size mismatch suggests a template variant (see recommendations). |
| Is the spec usable by a different implementation agent/model? | **Yes.** Spec + plan + tasks are self-contained in-repo with HANDOFF blocks; the independent reviewer (separate context) read and evaluated all three without the authoring conversation — practical evidence of cross-agent legibility. |
| Unresolved ambiguities blocking implementation? | **No.** The owner decision phase resolved U-1…U-13 (plus HOLD, U-5, U-12 and the v1 audio scope); `REMAINING_BLOCKING_PRODUCT_DECISIONS = NONE`. What remains is (a) **implementation authorization** — a separate owner gate, not a product decision — and (b) explicitly deferred, non-authorizing implementation details (spec §16). |
| Should the workflow/templates change before the next spec? | **Recommendations only** (not applied — a separate owner decision would be required): ① add an **experience-scale spec allowance** (core ≤ ~150 lines + labelled appendices) and an explicit rule for owner-mandated full artifact sets like this pilot's (spec+plan+tasks+assessment under a STANDARD work class); ② promote an **EVIDENCE** section/label into the spec template — separating observed evidence from requirements proved essential and should be first-class; ③ standardize a **Blocking owner decisions** table convention with IDs; ④ consider a lightweight pilot-assessment template so Stage-4-style evaluations are repeatable; ⑤ make the mobile-posture declaration (`WEB-SPECIFIC`/`CLIENT-AGNOSTIC`, ADR-0005 guardrail 3) an explicit template field — it was missed on first authoring and caught only by independent review. |
| Decision cycle (what the pilot actually proved) | **The pilot achieved its purpose.** (a) Spec Kit exposed hidden product assumptions *before* any coding: the pilot’s 13 blocking unknowns (U-1…U-13) became the owner-decision agenda, and no implementation churn occurred while they were resolved. (b) **Independent review caught real defects** — missing mobile-posture declaration, an unsupported citation, CONFIRMED-by-implementation rows — that authoring alone missed. (c) The **owner decision passes resolved every blocking item without changing code** — including the material **modularity decision** that reframed the experience as a composition of modules with an orchestration layer, and the **U-1 correction** (execution mode belongs to the prescription, not to exercise identity) that superseded the agent’s initial recommendation. (d) The **brownfield prototype stayed evidence**: prototype states, timings, and the 3D medium were consumed as evidence and explicitly *not* promoted to requirements. (e) **Deferred implementation details never blocked the product specification** — a clear separation of product decisions from implementation mechanics. |
| Legitimate friction (kept, not smoothed over) | ① The spec runs to **345 lines**, far above the STANDARD ≤~150-line guidance — the template has no experience-scale allowance. ② The mobile-posture declaration was missed on first authoring; only independent review caught it. ③ A partial owner delta (only §11–§12) initially arrived, producing a **recalculation that had to be declared invalid and redone** — a real cost of multi-part owner input delivery; the lesson is that decision deltas should be delivered whole (or explicitly marked partial). ④ The artifact-set exception (spec+plan+tasks+assessment under a STANDARD class) is still an exception, not a template feature. |
| Post-finalization independent review | **PASS after correction.** Review round 1 (consolidated spec, four artifacts) returned **FAIL** driven solely by `pilot-assessment.md`: 1 contradiction (stale “U-1…U-13 blocking / unresolved” rows contradicting `REMAINING_BLOCKING_PRODUCT_DECISIONS = NONE`) and 1 mis-described reference (a §14 “product-approved?” column that no longer exists). `spec.md`, `plan.md` and `tasks.md` were found **clean** (0 contradictions, 0 unsupported requirements, 0 hidden assumptions, 0 prototype leakage, prohibited-outcomes PASS). All findings were corrected in the same change; a verification pass then returned 0/0/0/0. The review checked specifically for: invented requirements · resolved items presented as blocking · prototype leakage · hidden 3D/Three.js mandate · fixed three-set or TIME_BASED-only assumptions · Intro repeated between same-exercise sets · REST owning Next-Exercise semantics · Do-Later mutating the prescription · Skip-Today marked completed/failed · silent COMPLETE with deferred blocks · audio required for comprehension · reduced-motion removing essential understanding · Voice/TTS entering v1 · accidentally authorized schema/implementation decisions · monolithic state-machine assumptions. |

## Verdict

`PASS` — the pilot produced a usable, evidence-separated, governance-conformant specification, and the decision cycle closed with **all blocking product decisions resolved and no implementation started**. Independent reviews: adoption-stage constitution review 0 contradictions / 0 duplicated authority; spec review round 1 (9 findings, all corrected); consolidated-spec review round 1 (infrastructure failure — no result; rerun); **round 2: spec/plan/tasks clean, `pilot-assessment.md` had 1 contradiction + 1 mis-described reference — both corrected**; round 3 verification 0/0/0/0. Two places where the standing workflow did not fit are recorded rather than silently bent: the **owner-mandate artifact-set exception** and the **artifact-size mismatch**. One process defect is recorded as a lesson: a partially delivered owner delta caused a recalculation that had to be invalidated and redone.

## Explicitly unchanged by this pilot

- No global template change; no governance-machine change (`governance-runtime.mjs` untouched).
- Constitution remains `PROPOSED / NON-BINDING`.
- No implementation authorized or started; the future implementation is expected CRITICAL-class and separately gated.
